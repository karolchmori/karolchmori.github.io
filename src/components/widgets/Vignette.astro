---
interface Props {
  opacity?: number;
}

const { opacity = 0.9 } = Astro.props;
---

<div class="vignette" style={`--vignette-opacity: ${opacity};`} />

<style>
  .vignette {
    position: fixed;
    inset: 0;
    pointer-events: none;

    background: radial-gradient(
      ellipse 200% 70% at 50% 58%,
      transparent 0%,
      transparent 70%,
      rgba(233, 155, 207, 0) 55%,
      rgba(12, 9, 12, 0.755) 80%,
      rgba(0, 0, 0, 0.912) 100%
    );

    opacity: var(--vignette-opacity);
    z-index: 100;
  }
</style>